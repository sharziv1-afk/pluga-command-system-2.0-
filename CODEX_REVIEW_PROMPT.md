# פרומפט ל‑Codex — ביקורת בלבד, ללא שינויים

> העתק את כל מה שמתחת לקו לתוך Codex.

---

## חוקים מוחלטים

1. **אל תשנה שום דבר.** אל תערוך קבצים, אל תיצור קבצים, אל תמחק, אל תריץ
   `git commit` / `git checkout` / `git stash` / `npm install`. אתה **קורא בלבד**.
2. **אל תריץ SQL ואל תיגע ב‑Supabase.** מסד הנתונים מכיל נתוני פלוגה אמיתיים.
3. אם אתה חושב שמצאת באג — **תתעד אותו, אל תתקן אותו.**
4. **חסוך בטוקנים.** אל תדפיס קבצים שלמים. קרא רק את מה שצריך, בקטעים.
   העדף `grep`/`rg` על פני קריאת קבצים.
5. **עצור לפני שנגמרים לך הטוקנים.** כשאתה מגיע ל‑~80% מהתקציב — **הפסק לחקור
   מיד** וכתוב את הדוח עם מה שיש לך. דוח חלקי שנכתב עדיף על חקירה מלאה
   שנקטעת. אל תתחיל בדיקה חדשה אם אתה לא בטוח שתספיק לכתוב את הדוח.

## הקשר

`C:\DEV\pluga-command-system` — Next.js 16 + Supabase + Tailwind 4, עברית RTL.
אפליקציית פיקוד פלוגתית בשימוש יומי אמיתי של מ״פ אחד.

עברנו תוכנית שיפור בת 6 שלבים. ההיסטוריה ב‑`git log`. המסמכים הרלוונטיים:
`DESIGN.md` (חוקי העיצוב), `QA_CHECKLIST.md`, `tests/`.

**קרא קודם:** `git log --oneline -20` — הודעות הקומיט מתארות במדויק מה נעשה
ולמה, כולל מה נשאר פתוח בכוונה. אל תדווח כבעיה על משהו שהודעת קומיט כבר
מסבירה כהחלטה מודעת.

## מה לבדוק — לפי סדר עדיפות

התחל מ‑1. אם נגמרים הטוקנים באמצע, עצור וכתוב דוח.

**1. נכונות — הכי חשוב**
- כתיבות ל‑Supabase שלא מוודאות שהשורה באמת נכתבה. הכלל: כל `.update()`
  ו‑`.delete()` חייבים `.select()` באותה שאילתה + בדיקת `didRowsUpdate`,
  כי PostgREST מחזיר הצלחה ריקה כשה‑RLS חוסם. (`.insert()` פטור.)
- סגירות תקועות (stale closures) ב‑React — במיוחד `setState` שמשתמש בערך
  שנתפס מחוץ ל‑callback במקום בצורה הפונקציונלית.
- `src/app/(protected)/forum/page.tsx` — הקובץ הכי רגיש. האינווריאנט:
  שיוך דוח למחלקה נעשה לפי `owner_user_id`, **לעולם לא לפי אינדקס במערך**.
- טיפול בשגיאות שמסתיר כשלון מהמשתמש.

**2. הרשאות ואבטחה**
- `src/lib/permissions.ts` מול מה שנשמר ב‑DB (אל תריץ SQL — קרא את הקוד
  ואת `supabase/migrations/`).
- `src/proxy.ts` — האם כל נתיב תחת `(protected)` מוגן.
- מפתחות/סודות בקוד צד‑לקוח.

**3. עקביות שמובילה לבאגים**
- קוד משוכפל שהתפצל (זה בדיוק מקור באג אזורי הזמן שתוקן — היו 6 עותקים
  של פונקציות תאריך ורק חלקם נעצו `Asia/Jerusalem`).
- מחרוזות שמוצגות למשתמש ולא עוברות דרך מודול תוויות משותף.

**4. בדיקות**
- `tests/` — האם יש בדיקה שלא מסוגלת להיכשל? (בודקת טקסט מקור במקום
  התנהגות, או מאמתת ספירה מדויקת שנשברת משינוי לגיטימי)
- מה לא מכוסה ומסוכן.

**5. ביצועים** — רק אם נשארו טוקנים. שים לב: הנתונים היום קטנים
(4 דרישות, 7 משימות, 13 מופעים, 12 חיילים). אל תדווח על "איטיות" שלא מדדת.

## מה **לא** לדווח

הדברים הבאים ידועים ומכוונים — אל תבזבז עליהם טוקנים:
- פיצול הקבצים הגדולים (מחכה לכיסוי בדיקות רחב יותר)
- 18 בדיקות ב‑`tests/stability.test.mjs` שסורקות טקסט מקור (תלוי בפיצול הנ״ל)
- שני `window.confirm` בפורום
- שני `<h1>` בכל עמוד
- חלון היצירה המהירה בלוח מפקד שנשאר פופאובר
- `formatDateTimeLocalInput` שלא נעוץ לאזור זמן (מכוון — צד הקריאה והכתיבה
  מסכימים זה עם זה)
- מסכי splash לאייפון (דולגו בהחלטה)
- 3 טבלאות עם RLS בלי policy (`approvals`, `feature_flags`,
  `onboarding_progress`) — דורש SQL ידני
- אזהרות `react-hooks/set-state-in-effect` (20, מכוון כאזהרה)

## פורמט הדוח

בסוף, כתוב **בעברית**, קצר וממוקד. בלי הקדמות ובלי סיכומי ביניים.

לכל ממצא:

```
### [P1/P2/P3] כותרת קצרה
קובץ: נתיב:שורה
מה קורה: משפט אחד.
למה זה בעיה: משפט אחד — מה המשתמש חווה בפועל.
איך אימתתי: מה קראת/הרצת שמוכיח את זה.
```

- **P1** — נכונות או אבטחה: המשתמש רואה מידע שגוי, מאבד נתונים, או נחשף מידע.
- **P2** — באג אמיתי בתרחיש קצה, או חוב שיהפוך לבאג.
- **P3** — שיפור.

**חובה:** אם לא הצלחת לאמת ממצא — כתוב `לא אומת` במפורש. אל תנחש. עדיף
3 ממצאים מאומתים מ‑15 השערות.

בסוף הדוח, שורה אחת: מה **לא** הספקת לבדוק.
