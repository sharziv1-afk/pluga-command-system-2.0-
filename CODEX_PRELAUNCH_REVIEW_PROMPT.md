# פרומפט ל‑Codex — סקירה שנייה, לפני עלייה ל‑Vercel

**להעתיק ולהדביק ל‑Codex כמו שזה. אל תשנה קוד, אל תריץ SQL, אל תיגע ב‑git.**

---

אתה עושה **סקירה קוראת בלבד** (read-only) של פרויקט Next.js 16 + Supabase
לפני שהוא עולה ל‑Vercel. **אל תשנה שום קובץ, אל תריץ `npm install` או פקודות
כתיבה, אל תיגע ב‑git.** המטרה: דוח ממצאים, לא תיקונים.

## הקשר

- אפליקציית ניהול פיקודי צבאית בעברית (RTL), בשימוש יומי אמיתי של מפקד יחיד
- Next.js 16 App Router, Supabase (Postgres + Auth + RLS), Tailwind v4
- `src/app/(protected)/forum/page.tsx` — הקובץ הכבד ביותר (~3,400 שורות),
  זרימת דוחות יומית עם הכרעת התנגשויות בין עורכים לפי היררכיית דרגה
- שכבת אופליין: IndexedDB לקריאה + תור כתיבות ל‑`tasks`/`forum_daily_reports`
  בלבד (עובדה ידועה, לא ממצא)

## זו סקירה שנייה — הראשונה מצאה שני חוסמי עלייה אמיתיים

**חלק א׳ של המשימה שלך: תאמת שהתיקונים נכונים באמת. אל תיקח את התיאור למטה
כעובדה — תקרא את הקוד בעצמך ותגיד אם זה באמת סגור.**

### חוסם 1 — דוח סגור נפתח מחדש משמירה מטאב ישן (טופל, לאימות)
היה: `submitSelectedReport` ומסלול השמירה היומית כתבו `status` מתוך
`selectedReport` — עותק שנטען כשהטיוטה נפתחה. במסלול ה‑merge (התנגשות),
זה עותק מיושן: טאב אחר יכול היה כבר לסגור את הדוח, וטאב זה "פותח אותו
מחדש" בטעות.

התיקון הנטען: `src/lib/forum/reportStatus.ts` — פונקציה `advanceReportStatus`
שמונעת נסיגת סטטוס (draft→in_progress→submitted→closed, אף פעם אחורה).
`writeWithHierarchyResolution` ב‑`src/lib/concurrency/hierarchyWrite.ts`
עודכן כך ש‑`buildPayload` מקבל פרמטר שני `currentRow` — השורה הטרייה
שנקראה מחדש, רק במסלול ה‑merge. שני מקומות ב‑`forum/page.tsx`, שורות
**1766** ו‑**1865**, אמורים להשתמש ב‑
`currentRow ? currentRow.status : selectedReport.status`.

**תבדוק:** האם `buildPayload` בשני המקומות באמת מקבל ומשתמש ב‑`currentRow`?
האם יש עוד מסלול כתיבה לטבלת `forum_daily_reports` שכותב `status` ולא
עבר דרך התיקון הזה? תבדוק את כל שלוש הקריאות ל‑`writeWithHierarchyResolution`
בקובץ, לא רק את השתיים שדווחו.

### חוסם 2 — מש״ד יכול "להפיץ ולסגור" חלקית ולקבל הודעת הצלחה (טופל, לאימות)
היה: הכפתור "הפצה וסגירת פורום" הוצג לכל מי שעומד ב‑`hasCompanyWideUiAccess`
(כולל מש״ד, permission_level 85), אבל מדיניות ה‑RLS `is_commander` דורשת
מ״פ/סמ״פ או דרגה 90+. מש״ד לא עומד בתנאי, אז ה‑bulk-close שלו נחסם חלקית
ע״י RLS וסוגר רק את הדוח שבבעלותו — וההודעה אמרה "הפורום הופץ ונסגר".

התיקון הנטען: משתנה חדש `canPublishForum` שמשקף בדיוק את `is_commander`
(שילוב של `hasAdminAccess` ו‑`profilePermissionLevel >= 90`), מציב את
הכפתור **וגם** את השומר בתוך `publishAndCloseForum` עצמה. בנוסף, השוואת
`closed.length` מול `openBefore` (לא רק מול אפס) לפני שמציגים הודעת הצלחה.

**תבדוק:** האם `canPublishForum` באמת שונה מ‑`canSeeAll` בכל מקום שצריך?
תבדוק אם יש עוד מקום בקובץ שמסתמך על `canSeeAll` לפעולה שדורשת בפועל
הרשאת `is_commander` במסד (לא רק ראייה, אלא כתיבה רחבה).

## חלק ב׳ — שלושה ממצאים שהוחלט במכוון **לא** לתקן עדיין. תסגור להם היקף מדויק

לא ביקשתי תיקון — ביקשתי **הבנה מדויקת של הסיכון**, כדי להחליט אם זה
מחכה או לא:

1. **שדות `datetime-local` מפורשים לפי אזור הזמן של המכשיר, לא `Asia/Jerusalem`**
   (דווח במסכי לו״ז/משימות/דשבורד/`MentoringPanel`). תמצא **את כל** המקומות
   הנוספים בקוד עם אותה תבנית (לא רק אלה שכבר דווחו), ותגיד: כמה מסכים
   נפגעים בפועל, ומה התרחיש המדויק שבו מפקד עם מכשיר שלא מוגדר לישראל
   רואה שעה שגויה.
2. **תקרת ברירת המחדל של 1,000 שורות ב‑Supabase select, בטבלת המעקבים**
   (`tracking/page.tsx`). כמה רחוק אנחנו מהתקרה הזו כרגע (כמה חיילים ×
   כמה פריטי מעקב, לפי מה שאתה רואה בקוד/סכימה)? זו בעיה של החודש הקרוב
   או של השנה הבאה?
3. **הודעות "נסה שוב בעוד רגע" גנריות שנשארו** ב‑`MentoringPanel.tsx`,
   `admin/page.tsx`, ומקום אחד ב‑`requests/page.tsx` (שורה ~594) — כתיבות
   ישירות שלא עוברות דרך תור האופליין, שמציגות הודעת תקלה כללית גם כשהסיבה
   היא ניתוק רשת. יש כבר helper מוכן ב‑`src/lib/offline/writeErrors.ts`
   (`writeFailureMessage`) שמשמש במקומות אחרים (tasks/schedule/requests) —
   תוודא שהוא באמת חסר בשלושת המקומות האלה ולא רק חלקית.

## חלק ג׳ — המשך הסקירה המקורית, בקטגוריות שעוד לא מוצו

### תקינות לוגית בכתיבות (מעבר לשני החוסמים)
- מלבד `forum_daily_reports`, יש עוד טבלה עם מדיניות RLS מדורגת (owner
  update own + commander update all) שבה ה‑UI מניח הרשאה רחבה יותר ממה
  שה‑DB בפועל נותן לתפקיד מסוים? תבדוק `tasks`, `requests`, `gaps`,
  `tracking_records`, `tracking_weeks` באותה שיטה בדיוק שחשפה את חוסם 2.
- כפתורי שמירה עם נעילת `isSubmitting`/`disabled` — יש עוד מסך חוץ מהפורום
  שמאפשר לחיצה כפולה מהירה לגרום לשתי כתיבות מתחרות?

### אבטחה
- כל שימוש ב‑`.update()`/`.delete()` שעדיין **לא** מלווה ב‑`.select()`
  ובדיקת שורות — לרענן את הסריקה, כי חלק מהקוד השתנה הערב.
- `service_role` / מפתחות מוטבעים — לרענן, אותו קריטריון כמו קודם.

### ביצועים
- לרענן את בדיקת ה‑N+1 ולולאות `useEffect` על הקבצים ששונו הערב
  (`forum/page.tsx`, `dashboard/page.tsx`, `schedule/page.tsx`, `login/page.tsx`).

## מה **לא** לבדוק
- עיצוב/CSS/נגישות ברמת פיקסלים — עבר QA נפרד על מכשיר אמיתי
- טבלת `gaps` בממשק — הוסתרה בכוונה (שינוי מוצר)
- 20 אזהרות lint על `setState` בתוך `useEffect` — נבדקו, לא באגים
- `distDir`, `allowedDevOrigins`, משתני סביבה — נבדקו בסקירה הקודמת, תקינים

## פורמט הדוח

לכל ממצא חדש: **קובץ:שורה**, **מה בדיוק שגוי**, **תרחיש קונקרטי**, **חומרה**.
לכל אימות (חלק א׳): **תקין** / **עדיין שבור, כי...** / **תוקן חלקית, חסר...**
— במפורש, לא "נראה בסדר". לכל היקף (חלק ב׳): מספרים קונקרטיים, לא "משמעותי"
או "כדאי לבדוק".

**תקציב טוקנים מוגבל — אם נגמר לך הזמן, עצור ורשום מפורשות מה לא הספקת.**
