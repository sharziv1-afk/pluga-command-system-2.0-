# פרומפט ל‑Codex — סקירת קוד לפני עלייה ל‑Vercel

**להעתיק ולהדביק ל‑Codex כמו שזה. אל תשנה קוד, אל תריץ SQL, אל תיגע ב‑git.**

---

אתה עושה **סקירה קוראת בלבד** (read-only) של פרויקט Next.js 16 + Supabase
לפני שהוא עולה לראשונה ל‑Vercel, הערב. **אל תשנה שום קובץ, אל תריץ `npm install`
או פקודות כתיבה, אל תיגע ב‑git.** המטרה: דוח ממצאים, לא תיקונים.

## הקשר

- אפליקציית ניהול פיקודי צבאית בעברית (RTL), בשימוש יומי אמיתי של מפקד יחיד
- Next.js 16 App Router, Supabase (Postgres + Auth + RLS), Tailwind v4
- `src/app/(protected)/forum/page.tsx` הוא הקובץ הכבד ביותר (~3,400 שורות) —
  זרימת דוחות יומית עם הכרעת התנגשויות בין עורכים לפי היררכיית דרגה
- יש שכבת אופליין: IndexedDB לקריאה + תור כתיבות ל‑`tasks`/`forum_daily_reports`
  בלבד (לא לכל הטבלאות — זו עובדה ידועה, לא ממצא)
- `next.config.ts` כולל CSP, HSTS וכותרות אבטחה נוספות; `allowedDevOrigins`
  מזהה כתובות רשת אוטומטית (dev בלבד, לא משפיע על בילד)

## מה לבדוק, לפי סדר עדיפות

### 1. אבטחה — הכי חשוב
- כל שימוש ב‑`supabase.from(...).update()/.delete()` — **חייב** להיות
  מלווה ב‑`.select()` ובדיקת מספר השורות שהושפעו. PostgREST מחזיר HTTP 204
  ללא שגיאה כשמדיניות RLS חוסמת כתיבה — קוד שלא בודק את זה **חושב שהצליח
  כשלמעשה שום דבר לא נכתב**.
- כל קריאת RPC (`supabase.rpc(...)`) — מי יכול לקרוא לה, ומה קורה אם
  הקורא לא מזוהה/לא מאושר?
- האם יש מפתח `service_role` בקוד הצד־לקוח (`src/`) בכל צורה שהיא — קובץ,
  משתנה סביבה עם `NEXT_PUBLIC_`, מחרוזת מוטבעת? **זה חייב להיות ריק.**
- זריקת שגיאות ל‑console בקוד production — האם יש מקום שמדפיס אובייקט
  שגיאה גולמי (לא מסונן) מ‑Supabase שיכול לחשוף מבנה פנימי?

### 2. תקינות לוגית בכתיבות
- בכל מסך יצירה/עריכה: מה קורה אם הרשת נופלת **באמצע** השמירה? האם המשתמש
  רואה הודעה נכונה, או הודעה שמרמזת ש"תקלה זמנית, נסה שוב" כשבפועל אין רשת?
- קבצי `writeWithHierarchyResolution` / `hierarchyWrite.ts` — בדוק שההכרעה
  בין גרסאות מתנגשות עקבית: שדה שלא נגעו בו לא אמור "לנצח" שדה שכן נערך.
- מרוצי תנאים (race conditions) בכפתורי שמירה שניתן ללחוץ עליהם פעמיים
  ברצף מהיר — יש נעילה (`isSubmitting`/`disabled`) בכל מקום שכותב לשרת?

### 3. עקביות טיפוסים ו‑null handling
- שדות `| null` ב‑TypeScript מול קוד שמניח שהם תמיד קיימים (`user.name!`
  או גישה בלי בדיקה) — במיוחד ב‑`assigned_to`, `unit_id`, `event_id`.
- כל מקום שממיר תאריך/שעה — עקביות אזור זמן (`Asia/Jerusalem` בלבד,
  לא `new Date()` גולמי בהצגה למשתמש).

### 4. ביצועים לפני עומס אמיתי
- שאילתות `select('*')` על טבלאות עם הרבה שורות פוטנציאליות — יש `.limit()`?
- לולאות שמבצעות קריאת רשת אחת בכל איטרציה (N+1) במקום קריאה מרוכזת.
- קומפוננטות עם `useEffect` בלי מערך תלויות, או עם תלות שמשתנה בכל רינדור
  (אובייקט/מערך חדש כל פעם) — גורם ללולאת רינדורים.

### 5. תשתית הבנייה (רלוונטי הערב ספציפית)
- `next.config.ts` — `distDir: ".next-build"` לא סטנדרטי. יש משהו נוסף
  שתלוי בנתיב הזה חוץ מ‑`tsconfig.json`/`next-env.d.ts` (שכבר נבדקו ותקינים)?
- `middleware`/`proxy` (`src/proxy.ts`) — יש הנחה כלשהי על סביבת ריצה
  (Node vs Edge) שיכולה להישבר ב‑Vercel Edge Runtime?
- משתני סביבה: כל `process.env.X` בקוד — יש לו `NEXT_PUBLIC_` אם הוא
  נקרא גם בצד לקוח? חסר אחד כזה נכשל בשקט בפרודקשן בלי שגיאת build.

## מה **לא** לבדוק (כבר טופל, לא לבזבז זמן)
- עיצוב/CSS/נגישות ברמת פיקסלים — עבר סבב QA נפרד על מכשיר אמיתי
- מבנה תיקיות/ארכיטקטורה כללית — לא בשלב הזה
- טבלת `gaps` — הוסתרה מהממשק בכוונה (שינוי מוצר, לא באג)
- 20 אזהרות lint על `setState` בתוך `useEffect` — נבדקו, אותו כלל React,
  לא באגים

## פורמט הדוח

לכל ממצא: **קובץ:שורה**, **מה בדיוק שגוי** (לא "כדאי לשפר"), **תרחיש קונקרטי**
שבו זה נכשל (קלט/תנאי מדויקים → תוצאה שגויה), ו**חומרה** (חוסם עלייה /
כדאי לתקן / הערה). מיין מהחמור לקל. אם משהו נראה חשוד אבל לא בטוח — סמן
כ"לא ודאי" במפורש במקום להציג כממצא סופי.

**תקציב טוקנים מוגבל — אם נגמר לך הזמן, עצור ורשום מפורשות אילו קטגוריות
מהרשימה למעלה לא הספקת לבדוק**, כדי שנדע מה עדיין פתוח.
