# פרומפט ל-Codex — אימות וביקורת על עבודת Phase 2 (מערכת עיצוב), "המפקד"

הדבק את כל המסמך הזה ל-Codex כפי שהוא. הוא כתוב כדי לתת לך הקשר מלא בלי
תלות בשיחה קודמת.

## מי אתה ומה המשימה

אתה עובד על `pluga-command-system` ("המפקד") — מערכת פיקוד פלוגתית בעברית
RTL, Next.js 16 + Supabase, בשימוש יומי אמיתי (המ״פ נכנס מהטלפון שלו מול
פרויקט LIVE `vmfihyritfmjycrfpxjn`). **המשימה שלך: לבקר, לאמת ובמידת
הצורך לתקן** סבב עבודה שכבר בוצע ונדחף ל-`main` — לא לבנות משהו חדש.

**גישה נדרשת:** ביקורתית ועצמאית. אל תניח שמה שנכתב למטה נכון רק כי זה
כתוב — תבדוק בעצמך מול הקוד החי, מול `npm run check`, ומול הדפדפן. אם
משהו שגוי — תקן אותו ישירות בקוד, בקומיט נפרד עם הודעה ברורה שמסבירה מה
היה שגוי ולמה. זו בדיוק הסיבה שאתה מופעל: מודל אחר (אני) עשה את העבודה,
ואתה אמור לתפוס טעויות שהוא לא תפס.

## הקשר: מה זה "שלב 2" ולמה זה קיים

התוכנית המאושרת לפרויקט (`שלב 2` מתוך תוכנית השקה רחבה יותר) קבעה: **אותו
זהות ויזואלית, ברמת ביצוע גבוהה יותר** — בלי שינוי ערכת צבע, בלי בורר
ערכות למשתמש (הרעיון הזה נבדק ונדחה במפורש — עלות QA גבוהה מדי לתועלת
בכלי פנימי ל-20 משתמשים). המשתמש אישר לי (הסוכן שעבד לפניך) "לרוץ עד סוף
שלב 2" עם checkpoints (עדכון + הדגמה חיה אחרי כל צעד גדול, לא אישור מראש
לכל שינוי).

## המקורות ששימשו לעבודה — תבדוק שהם אכן שימשו נכון

1. **הקוד הקיים עצמו** — `src/app/globals.css`, `src/components/ui/*` —
   נקרא לעומק לפני כל שינוי (למשל: התגלה ש-Dark Mode כבר קיים ועובד
   במלואו — `ThemeToggle.tsx` — והיה רק צריך להשלים אותו, לא לבנות מחדש).
2. **`.agents/skills/impeccable/reference/*.md`** — קובצי ידע קיימים
   בריפו (typeset.md, colorize.md, interaction-design.md, craft.md ועוד)
   עם כללים קונקרטיים (סולם טיפוגרפי 5 גדלים ביחס ~1.2, לא יותר מ-3-4
   משקלי גופן, `:focus-visible` לא `:focus`, `<dialog>` native ל-modals,
   OKLCH/ניגודיות/dark-mode-is-not-inverted-light-mode וכו׳). **תבדוק
   שההחלטות ב-`DESIGN.md` (ראה למטה) באמת עוקבות אחרי הכללים האלה.**
3. **`https://styles.refero.design`** — גלריית DESIGN.md לדוגמאות מאתרים
   אמיתיים. שימש רק לוודא שהמבנה/הפורמט של `DESIGN.md` שכתבתי תואם למה
   שכלי AI אחרים מצפים לו — **לא** נלקח ממנו שום החלטת צבע/טיפוגרפיה
   בפועל (המוצר נשאר על הזהות הקיימת שלו).
4. **`21st` CLI** (`21st init --design-context --refresh`) — לרענון
   `.21st/design.json`. **חשוב לבדוק:** ה-refresh לא באמת פתר את הבאג
   המקורי (ראה סעיף 5 למטה) — תוקן ידנית אחרי זה. תוודא שהתיקון הידני
   נכון.

## מה בדיוק השתנה — 4 קומיטים, מ-`a7308f8` עד `HEAD` ב-`main`

הרץ `git log --oneline a7308f8..HEAD` ו-`git diff --stat a7308f8..HEAD`
כדי לראות את זה בעצמך. תקציר של כל קומיט, מהראשון לאחרון:

### קומיט 1 — `fd89ad8`: תשתית (DESIGN.md, טיפוגרפיה, ניגודיות גבוהה, איחוד רכיבים)

- **`DESIGN.md` חדש** בשורש הריפו — קרא אותו במלואו קודם, הוא מקור האמת
  לכל שאר הביקורת. הכלל המרכזי: `--brand` (#ff6b02) דקורטיבי בלבד (נכשל
  AA כטקסט/רקע אינטראקטיבי), `--action` (#c2410c) הוא הצבע האינטראקטיבי
  היחיד. **תבדוק בעצמך**: האם `--action` אכן עומד ב-4.5:1 מול לבן, והאם
  `--brand` אכן נכשל — אל תסמוך על המספרים שכתבתי (5.18:1 / ~2.9:1),
  תחשב contrast ratio בעצמך.
- **סולם טיפוגרפיה** — `.text-caption/.text-meta/.text-body-ui/.text-subheading/.text-heading`
  ב-`src/app/globals.css` (חפש "Typography scale"). **החלטה מכוונת:**
  נבנה כמחלקות CSS ידניות, **לא** דרך ה-namespace של Tailwind v4
  `--text-*` ב-`@theme` — כי זה היה מתנגש עם טוקני הצבע הקיימים
  `--text-primary/--text-secondary/--text-muted-accessible`. **תבדוק**
  שאין באמת התנגשות/דריסה של אף טוקן קיים ב-`@theme` וב-`:root`.
- **ניגודיות גבוהה** — `:root[data-contrast="high"]` ב-`globals.css` +
  `src/components/layout/ContrastToggle.tsx` (חדש, מבנה זהה בכוונה ל-
  `ThemeToggle.tsx` הקיים) + חוברו ל-`AppSidebar.tsx`/`MobileHeader.tsx`.
  **תבדוק**: שהטוגל באמת עובד (localStorage key `pluga_contrast`,
  data-attribute על `document.documentElement`), ושה-CSS override מכסה
  גם רכיבים שה-border-color שלהם היה **hardcoded** ולא דרך טוקן (למשל
  `.command-input`) — יש הערה בקוד על זה, תוודא שהיא נכונה.
- **`StatusBadge.tsx` + `EmptyState.tsx`** — מיגרציה מלאה מ-hex/Tailwind
  raw colors לטוקנים סמנטיים. תבדוק את `getStatusStyles()` ב-
  `StatusBadge.tsx` — כל סטטוס בעברית ממופה לטון סמנטי (success/info/
  warning/danger/neutral) — **תבדוק שהמיפוי הגיוני** (למשל "ממתין לאישור"
  → warning, לא danger או neutral).
- **`GlossyButton.tsx`** — הפך ל-wrapper דק סביב `CommandButton.tsx` (לא
  עוד מימוש כפול). **תבדוק**: שכל 13 האתרים שמשתמשים ב-`GlossyButton`
  עדיין מקבלים בדיוק את אותה חוויה ויזואלית (מיפוי `variant`:
  `cyan→teal`, `orange→primary`, `slate→subtle`).
- **3 מתוך 6 `window.confirm`** הוחלפו ב-`CommandConfirmDialog` (מחיקת
  משימה ב-`tasks/page.tsx`, מחיקת מופע ב-`schedule/page.tsx`, איפוס+מחיקת
  דוח ב-`forum/page.tsx`). **תבדוק כל אחד**: שה-state שנוסף (למשל
  `taskPendingDelete`) נסגר נכון אחרי confirm/cancel, ושאין race
  condition בין הדיאלוג לבין הלוגיקה האסינכרונית שמתחתיו.
- **הוחלט במכוון לא לגעת** בשני `window.confirm` בפורום
  (ב-`requestDailyScopeTransition`, אזורי "יש שינויים שלא נשמרו") — הם
  חלק מזרימת בקרה סינכרונית (`canTransitionDraft`) שיש עליה בדיקות
  ייעודיות ב-`tests/stability.test.mjs`, והפיכתם לדיאלוג אסינכרוני היא
  שינוי התנהגותי, לא רק עיצובי. **תבדוק**: שההחלטה הזו הגיונית, ואם יש
  לך פתרון שממיר אותם **בלי** לשבור את הקונטרקט הסינכרוני של
  `canTransitionDraft` — אתה מוזמן להציע/לממש, אבל רק אחרי שהרצת את
  הבדיקות הרלוונטיות ווידאת שהן עדיין עוברות.
- **`.21st/design.json`** — תוקן באג אמיתי: הקובץ הצהיר `"colorMode": "light"`
  אבל כמה ערכים (`--color-app-bg`, `--text-primary` ועוד) היו בפועל ערכי
  ה-dark mode, כי כלי ה-scraper של `21st` לוקח את הערך **האחרון שמוגדר**
  לכל שם משתנה בקובץ ה-CSS כולו, בלי להתחשב בכך שהוא מוגדר מחדש בתוך
  `:root[data-theme="dark"]`. תוקן ידנית (`node -e` script, לא Edit tool
  — תבדוק את הערכים בעצמך מול `globals.css`'s `:root` הרגיל, לא ה-dark
  block). גם `constraints.must`/`constraints.avoid` מולאו ידנית — היו
  ריקים. **תבדוק**: שהערכים שתוקנו תואמים בדיוק למה שבאמת מוגדר ב-`:root`
  (הלא-dark) ב-`globals.css`.

### קומיט 2 — `8ffb676`: modal מגולף ביד ב-tracking

- `src/app/(protected)/tracking/page.tsx` — modal אישור מחיקה (חייל/פריט
  מעקב) שהיה בנוי ידנית (`fixed inset-0` + listener ידני ל-Escape) הוחלף
  ב-`CommandConfirmDialog`. Escape listener היד-ני הוסר (מיותר עם
  `<dialog>` native).
- **באג אמיתי שנתפס ותוקן בו-במקום**: `CommandConfirmDialog`'s
  `description` prop הורחב מ-`string` ל-`React.ReactNode` כדי לתמוך
  בתוכן רב-פסקאות. זה חשף שה-wrapper הפנימי עטף את זה ב-`<p>` — וכש-
  tracking העביר fragment שמכיל בעצמו `<p>`, נוצר `<p><p>...` לא תקין
  (hydration error אמיתי, נתפס ב-console אחרי בדיקה חיה בדפדפן, לא
  בבדיקות האוטומטיות). תוקן ע״י שינוי ה-wrapper ב-`CommandDialog.tsx`
  מ-`<p>` ל-`<div>`. **תבדוק**: שאין עוד מקום בקוד שמניח שה-wrapper הוא
  `<p>` (חיפוש `text-sm leading-relaxed text-\[var(--text-secondary)\]`),
  ושכל הקריאות הקיימות ל-`CommandConfirmDialog` (5 מקומות) עדיין תקינות
  עם ה-wrapper כ-`<div>`.

### קומיט 3 — `4a59a49`: שני modals מגולפים ביד בפורום

- `src/app/(protected)/forum/page.tsx` — אישור "רענון דוח פלוגתי" ואישור
  "הפצה וסגירת פורום" הוחלפו ב-`CommandConfirmDialog`, אותו דפוס בדיוק.
  **הערה חשובה לבדיקה שלך**: אלה **לא** נבדקו חי בדפדפן (לא נמצא מסלול
  UI מהיר להגיע למצב שבו הכפתורים המפעילים אותם גלויים — דורש להיכנס
  לעריכת הדוח הפלוגתי). האימות היחיד היה שקילות מבנית לדפוס שכבר אומת
  פעמיים (tasks, tracking) + `npm run check` ירוק. **זו בדיוק המשימה
  שלך: תמצא את המסלול ב-UI (כנראה: פורום → הדוח הפלוגתי של המ״פ → מצב
  עריכה), תפעיל את שני הדיאלוגים בפועל, ותוודא שהם עובדים.** אל תלחץ על
  כפתור האישור בפועל של "הפץ וסגור" על נתוני LIVE אמיתיים בלי לוודא
  קודם עם המשתמש — זו פעולה הרסנית שסוגרת את כל דוחות היום לכלל הפלוגה.

### קומיט 4 — `57ef4a4`: מיגרציית טוקנים לדשבורד (העמוד הראשון מתוך 6)

- `src/app/(protected)/dashboard/page.tsx` — **זה הקומיט עם הכי הרבה
  שיפוט אנושי/החלטות הקשר, ולכן הכי כדאי לבדוק לעומק:**
  - 6 ערכי hex (`#FF6B02`, `#020108`, `#667085`, `#E65F00`, `#C54F00`,
    `#344054`) הוחלפו בטוקנים. **ההחלטה העדינה**: אותו hex `#FF6B02`
    מופיע גם בהקשרים דקורטיביים (אייקונים, badges) וגם בהקשרים
    אינטראקטיביים אמיתיים (רקע כפתור CTA ראשי, hover על קישורים, מצב
    "פעיל" של toggle). הראשונים הפכו ל-`var(--brand)`, השניים ל-
    `var(--action)`/`var(--action-hover)`. **תבדוק כל שימוש בקובץ**
    (`grep -n "var(--brand)\|var(--action)" src/app/(protected)/dashboard/page.tsx`)
    ותשפוט אם ההחלטה נכונה בכל מקום — יש כאן מקום אמיתי לטעות שיפוטית.
  - `toneClasses` (מפה פנימית טון→צבע, מקבילה ל-`MetricCard`'s
    `toneStyles`) נבנתה מחדש על טוקנים סמנטיים.
  - **21 מופעי `font-black` נבדקו אחד-אחד** (לא replace גורף) ומופו
    לפי טבלת המשקלים ב-`DESIGN.md` §2.2: 20 הפכו ל-`font-semibold`
    (כותרות כרטיס/פריט, לא כותרת עמוד אמיתית), 1 (כפתור ה-CTA הראשי
    בטופס יצירה מהירה) הפך ל-`font-bold`, ומספר ה-KPI האמיתי היחיד
    (`SummaryCard`'s value) עבר למחלקה המשותפת `.command-kpi` (משקל 700
    מובנה) במקום `font-black` + `leading-none` ידניים. **תבדוק**: שאתה
    מסכים עם השיפוט הזה — האם יש עוד "כותרת עמוד אמיתית" שפספסתי שצריכה
    להישאר/להפוך ל-`font-bold` ולא `font-semibold`?
  - 8 גדלי `text-[Npx]` שרירותיים מופו ל-`.text-caption`/`.text-meta`.

## מה עוד **לא** בוצע — אל תניח שזה גמור

**שלב 2 רחוק מלהיות שלם.** אל תדווח "הכל תקין" בלי לציין את זה:

1. **5 מתוך 6 העמודים הכבדים עדיין לא עברו מיגרציית טוקנים/משקלים**:
   `forum/page.tsx` (~3,400 שורות!), `schedule/page.tsx`,
   `requests/page.tsx`, `tracking/page.tsx`, `tasks/page.tsx`. הרץ
   `grep -c "font-black" src/app/\(protected\)/<page>/page.tsx` ו-
   `grep -oE "#[0-9A-Fa-f]{3,6}" src/app/\(protected\)/<page>/page.tsx | wc -l`
   על כל אחד כדי לראות את היקף מה שנשאר.
2. **3 modals מגולפים ביד עדיין לא הומרו**: יצירת/עריכת משימה
   (`tasks/page.tsx`), שני טפסי יצירה/עריכה ב-`schedule/page.tsx`,
   ו-popover ה"יצירה מהירה" בדשבורד (`dashboard/page.tsx` —
   `QuickCreateModal`, שהוא בכוונה **לא** modal ממורכז אלא popover
   מעוגן לכפתור עם חץ — הומלץ שלא להמיר אותו ל-`CommandOverlay` כמו
   שהוא כי זה ישנה את מיקום/UX שלו, לא רק את הסגנון. תבדוק אם אתה מסכים).
3. **עקביות טרמינולוגית (2.6 בתוכנית)** — "בקשה" מול "דרישה" מול "פערים"
   וכו׳ — לא נגעו כלל.
4. הטוגל `ThemeToggle` לא קורא `prefers-color-scheme` בטעינה ראשונה
   (תמיד מתחיל ב-light) — לא תוקן, לא היה בסקופ.

## מה לבדוק בפועל — checklist מסודר

1. **`npm run check`** מהשורש — חייב לצאת ירוק לגמרי (30/30 tests, lint,
   typecheck, build). אם משהו נכשל — זו כשלון קריטי, תקן לפני כל דבר
   אחר.
2. **קרא את `DESIGN.md`** במלואו, ותשפוט אם הוא עצמו עקבי/הגיוני — לא רק
   אם הקוד תואם לו.
3. **חפש עוד hex/font-black שנשארו בקבצים שכן נגעתי בהם** (לא בעמודים
   שעוד לא טופלו — שם ידוע שיש הרבה):
   `grep -rn "#[0-9A-Fa-f]\{3,6\}" src/components/ui/StatusBadge.tsx src/components/ui/EmptyState.tsx src/components/ui/GlossyButton.tsx`
   — אמור לצאת ריק.
4. **בדיקה חיה בדפדפן** (יש MCP browser tools; ההתחברות היא OTP למייל
   `sharziv1@gmail.com` — יש MCP של Resend לקריאת המייל האחרון שנשלח
   ולשליפת הקוד, בדיוק כמו שנעשה בסבב הזה):
   - התחבר, בדוק את `/dashboard` ב-light, dark (`ThemeToggle` בסיידבר),
     וניגודיות גבוהה (`ContrastToggle` לידו) — האם יש רגרסיה חזותית
     בכל אחד מהמצבים?
   - פתח את דיאלוג מחיקת המשימה ב-`/tasks`, מחיקת מופע ב-`/schedule`,
     ואיפוס/מחיקת דוח ב-`/forum` — **בטל בלבד, אל תאשר בפועל** (נתוני
     LIVE אמיתיים).
   - פתח את דיאלוג מחיקת חייל/פריט ב-`/tracking` — אותו דבר, בטל בלבד.
   - נסה להגיע לדיאלוגי "רענון דוח פלוגתי"/"הפצה וסגירה" בפורום (ראה
     הערה בקומיט 3 למעלה) ותאמת אותם.
5. **בדוק נגישות בפועל**: contrast ratio של `--action` על רקע לבן,
   ושל טקסטים בכל שלושת המצבים (light/dark/high-contrast) — אל תסמוך
   על המספרים שצוינו, תמדוד.
6. **אם מצאת טעות אמיתית — תקן אותה ישירות**, הרץ `npm run check` שוב,
   ותעד את התיקון בקומיט נפרד עם `git log`-style הסבר של מה היה שגוי,
   למה, ואיך תוקן — באותו סגנון שהקומיטים הקיימים כתובים (תראה
   `git log a7308f8..HEAD` לדוגמה של הסגנון המצופה).

## מגבלות — מה לא לגעת בלי לשאול

- אל תשנה את זהות הצבע (`--brand`/`--action` עצמם) — זו החלטה מאושרת.
- אל תוסיף בורר ערכות/צבע אישי למשתמש — נדחה במפורש.
- אל תיגע ב-Vercel או תעשה deploy — שער נפרד שדורש אישור שלב-שלב מהמשתמש.
- אל תבצע commit/push בלי לוודא ש-`npm run check` ירוק לגמרי קודם.
- אם אתה לא בטוח אם תיקון מסוים משנה התנהגות (לא רק סגנון) — עצור ותשאל,
  אל תנחש. הפרויקט הזה נמצא בשימוש חי יומיומי אמיתי.
